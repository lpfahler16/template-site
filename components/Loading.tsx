const Loading: React.FC = ({}): JSX.Element => (
  <div>
    <main className="bg-darkest-gray min-h-screen overflow-hidden flex flex-col justify-center items-center w-full">
      <p className="text-lightest-gray">Loading</p>
    </main>
  </div>
);

export default Loading;
